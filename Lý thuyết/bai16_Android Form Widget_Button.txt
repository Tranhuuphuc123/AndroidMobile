********************TIỀM HIỂU VỀ ANDROID FORM WIDGET_BUTTON*******************
>>>>KHÁI NIỆM:
  - Button trong Android là một thành phần giao diện người dùng cho phép người dùng tương tác
  bằng cách nhấn vào nó. Nó thường được sử dụng để kích hoạt một hành động hoặc mở ra một chức
  năng mới trong ứng dụng.

************************************
>>>>>CÁC THUỘC TÍNH
 => nó chia sẻ chung các thuộc tính của view/textview...
 => có các thuộc tính đặc trưng:
  ##1/ android:onClick: 
   => Xác định phương thức trong Activity sẽ được gọi khi Button được nhấn.
   => vd:
             android:onClick="onClickButton"

         => sử dụng android:onClick trong XML layout, ta có thể gán phương thức
         onClick này cho một Activity (.java) để xử lý sự kiện như sau:

            /tạo method onClickButton để xử lý sự kiện khi nhấn button
            public void onClickButton (View v){
                // tiến hành tạo content chứa nd nhận đc khi nhâp từ bàn phím vào editText
                String content = editInput.getText().toString();
                // tiến hành gán content cho txtResult để hiển thị ra TextView sau khi nhấn nút button
                txtResult.setText(content);

                //-> có thể viết gọn: txtResult.setText(editInput.getText().toString());
             }

  ##2/ android:enabled: 
   => Cho phép hoặc vô hiệu hóa Button. 
   => true cho phép, false là vô hiệu hóa


*****************************************
>>>>>CODE MINH HỌA:
 => code xử lý bên layout

            <?xml version="1.0" encoding="utf-8"?>
            <LinearLayout
                xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:padding="20dp">

                <Button
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:drawableLeft="@drawable/mario"
                    android:drawablePadding="10dp"
                    android:text="Show Context"
                    android:textSize="30dp"
                    android:textAllCaps="true"
                    android:background="@color/green"
                    android:textColor="@color/white"
                    android:layout_marginBottom="50dp"
                    android:textStyle="bold|italic"
                    android:gravity="center_vertical" />

            </LinearLayout>




     
********************************************************************************
>>>>>BÀI TẬP ỨNG DỤNG:
 ## FILE LAYOUT
        <?xml version="1.0" encoding="utf-8"?>
        <LinearLayout
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="20dp">

            <EditText
                android:id="@+id/edit"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="text"
                android:hint="Your Text"
                android:textSize="20dp"
                android:padding="10dp"
                android:textColor="@color/red"
                android:layout_marginBottom="20dp"/>

            <Button
                android:id="@+id/btn"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Show Content"
                android:textAllCaps="true"
                android:textSize="20dp"
                android:textStyle="bold|italic"
                android:padding="10dp"
                android:gravity="center_horizontal"
                android:layout_marginBottom="20dp"
                android:onClick="showContent"/>

            <TextView
                android:id="@+id/txt"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="20dp"
                android:background="@color/grey"
                android:padding="10dp"/>

        </LinearLayout>

 
 ===========================================
 ## FILE ACTIVITY XỬ LÝ SỰ KIỆN JAVA
          
              package com.example.tonghopkienthuc;

              import androidx.appcompat.app.AppCompatActivity;

              import android.os.Bundle;
              import android.widget.Button;
              import android.widget.EditText;
              import android.widget.TextView;
              import android.widget.Toast;
              import android.view.View;


              public class Baitapungdung1Activity extends AppCompatActivity {

                  /*tiến hành xử lý sự kiện bền Activity java file*/

                  // tạo các properties ánh xạ bên layout qua bên Activity .java
                  private EditText editInput;
                  private Button btnShow;
                  private TextView txtResult;

                  @Override
                  protected void onCreate(Bundle savedInstanceState) {
                      super.onCreate(savedInstanceState);
                      // liên kết với file layout
                      setContentView(R.layout.activity_baitapungdung1);

                //tiến hảnh kết nối các properties trên với các id bên layout để kết nối layout với Activity .java
                      editInput = (EditText)findViewById(R.id.edit);
                      btnShow=(Button) findViewById(R.id.btn);
                      txtResult= (TextView) findViewById(R.id.txt);
                  }

                  //tạo method showcontext để xử lý sự kiện khi nhấn button
                  public void showContent (View v){
                      // tiến hành tạo content chứa nd nhận đc khi nhâp từ bàn phím vào editText
                      String content = editInput.getText().toString();
                      // tiến hành gán content cho txtResult để hiển thị ra TextView sau khi nhấn nút button
                      txtResult.setText(content);

                      //-> có thể viết gọn: txtResult.setText(editInput.getText().toString());
                  }

                  // tạo method show Toast=> có nhiệm vụ thông báo xuất hiện Toast trên mobile
                  public void showToast(String msg){
                      Toast.makeText(getApplicationContext(),msg, Toast.LENGTH_LONG).show();
                  }
              }


